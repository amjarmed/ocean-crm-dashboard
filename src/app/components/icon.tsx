// // components/Icon.js
// import * as Icons from 'lucide-react';

// const MyIcon = ({ iconName, size = 24, color = 'currentColor' }) => {
//   // Dynamically choose the icon from the lucide-react icons library
//   const LucideIcon = Icons[iconName];

//   // If the icon doesn't exist, return null or some fallback UI
//   if (!LucideIcon) {
//     return <span>Icon not found</span>;
//   }

//   return (
//     <LucideIcon
//       size={size}
//       color={color}
//     />
//   );
// };

// export default MyIcon;
